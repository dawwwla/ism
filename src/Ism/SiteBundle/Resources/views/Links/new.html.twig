{% extends "IsmSiteBundle::Links/links.html.twig" %}

{% block ismsite_body -%}
    <h2>Ajouter un nouveau lien</h2>

    <form action="{{ path('links_new') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <a href="{{ path('ismsite_links') }}" class="btn">
              <i class="icon-chevron-left"></i>
              Retour à la liste
            </a>
            <button type="submit" class='btn'>Valider</button>
        </p>
    </form>

{% endblock %}
